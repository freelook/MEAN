define(['angularAMD', 'angularRoute', 'angularResource'], function(angularAMD, angularRoute, angularResource){
	angular.module("app.controllers", []).controller("navCtrl", ["$scope", function($scope) {
//Todo nav
		console.info('nav');
		}]);
});
